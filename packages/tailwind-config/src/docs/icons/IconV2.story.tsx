import { Meta, StoryObj } from '@storybook/react/*'
import { IconClasses } from './IconClasses'

const meta: Meta = {
  title: 'tailwind-config/Icons/IconV2',
  component: IconClasses,
}
export default meta

type Story = StoryObj<typeof IconClasses>

export const Playground: Story = {}
